h1
  | LTI Course Configurations

.table
  table.table.table-striped
    thead
      tr
        th
        th Name
        th URL
        th Consumer Key
        th Edit
        th Delete

    tbody
      - @courses.each do |course|
        tr
          td.vert-align = course.id
          td.vert-align = link_to "#{course.name}", course
          td.vert-align = course.url
          td.vert-align = course.consumer_key
          td.vert-align = link_to 'Edit', edit_course_path(course)
          td.vert-align = link_to raw('<strong>&times;</strong>'), course, class: "btn btn-danger", data: {:confirm => 'Are you sure?'}, :method => :delete

br
a.btn.btn-success(href=new_course_path role='button')
  | New Course Configuration