p#notice
  = notice
.hidden(style='display: none')
  = link_to 'Back', problems_path
  = form_tag(submission_problem_path(params[:id]), method: "post") do
    = text_field_tag(:score)
    = submit_tag("Submit")
= render 'snap_base'
= render 'snap_autograder'
script
   == @problem.tests
